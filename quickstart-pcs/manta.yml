services:
  manta:
    image: tgrivel/manta-ws:0.1.13
    hostname: manta-ws
    container_name: manta-ws
    restart: always
    networks:
      - internal
      - jwt-internal
    volumes:
      - ./configs-manta:/root/.config/manta
    ports:
      - 3000:3000

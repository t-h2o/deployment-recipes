services:
  manta:
    image: tgrivel/manta-ws:0.6.0
    hostname: manta-ws
    container_name: manta-ws
    restart: always
    networks:
      - internal
      - jwt-internal
    volumes:
      - ./configs-manta:/root/.config/manta
    ports:
      - 3000:3000
